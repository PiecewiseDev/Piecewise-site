import React from 'react';
import {
  BuildIntro,
  BuildChecklist,
  BuildUseCases,
  BuildAddOns,
  BuildComparison,
} from '@/sections/services';
import { redirect } from 'next/navigation';

export default function ServicesPage() {
  // Redirect to home page - services page temporarily archived
  redirect('/');

  // Services page temporarily archived
  // Original implementation code removed
}
